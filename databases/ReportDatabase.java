public class ReportDatabase extends Database{
  /* Description: Centralized storage for all reports
   * Responsibilities are covered in Database superclass where entries are reports
   */
   
  //addEntry(Report r);
  //removeEntry(Report r);
  //editEntry(Report r);
}
