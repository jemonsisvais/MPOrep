public class UserDatabase extends Database{
  /* Description: Centralized storage for all user information
   * Responsibilities are covered in Database superclass where entries are users
   */
   
  //addEntry(User u);
  //removeEntry(User u);
  //editEntry(User u);
}
